"""The optimization agent: `CostModelAgent` and everything it's built from
(cost-model plumbing, the cost-helper agent, sandbox tools, the LLM client, and
the physical-pipeline plan representation it operates over)."""
